apiVersion: v1
kind: Service
metadata:
  name: sample-app
  labels:
    app: sample-app
    managed-by: driftlens
spec:
  selector:
    app: sample-app
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
